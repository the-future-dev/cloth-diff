"""
Foundation model style policies (e.g. transformers without diffusion).

This package contains general sequence / image encoder policy implementations
that produce actions directly (supervised behavioral cloning) rather than
sampling from a diffusion process.
"""
